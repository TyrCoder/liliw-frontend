node_modules/
.env
.env.local
dist/
build/
.cache

# Local agent settings — machine-specific, not part of the project.
.claude/

# Local database backups (full table exports, taken before/after schema
# changes) — real user data, never belongs in version control.
db-backups/
