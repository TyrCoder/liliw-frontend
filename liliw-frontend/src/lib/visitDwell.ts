// Shared between the client (attraction detail page) and the server
// (visit/checkin routes) so the dwell requirement can't drift between them.
export const VISIT_DWELL_MS = 150_000; // 2.5 minutes
